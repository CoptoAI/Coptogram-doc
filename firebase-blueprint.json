{
  "entities": {
    "section": {
      "title": "Documentation Section",
      "description": "A top-level category of documentation.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "category": { "type": "string", "enum": ["organization", "student", "general"] },
        "icon": { "type": "string" },
        "content": { "type": "string" },
        "tags": { "type": "array", "items": { "type": "string" } },
        "order": { "type": "number" },
        "translations": {
          "type": "object",
          "properties": {
            "ar": {
              "type": "object",
              "properties": {
                "title": { "type": "string" },
                "content": { "type": "string" },
                "tags": { "type": "array", "items": { "type": "string" } }
              }
            }
          }
        }
      },
      "required": ["id", "title", "category", "content"]
    },
    "item": {
      "title": "Documentation Item",
      "description": "A specific article or guide within a section.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "details": { "type": "array", "items": { "type": "string" } },
        "version": { "type": "string" },
        "code": { "type": "string" },
        "tags": { "type": "array", "items": { "type": "string" } },
        "order": { "type": "number" },
        "translations": {
          "type": "object",
          "properties": {
            "ar": {
              "type": "object",
              "properties": {
                "title": { "type": "string" },
                "description": { "type": "string" },
                "details": { "type": "array", "items": { "type": "string" } },
                "tags": { "type": "array", "items": { "type": "string" } }
              }
            }
          }
        }
      },
      "required": ["id", "title", "description"]
    },
    "feedback": {
      "title": "Feedback Entry",
      "description": "User feedback for documentation pages.",
      "type": "object",
      "properties": {
        "type": { "type": "string", "enum": ["positive", "negative"] },
        "comment": { "type": "string" },
        "sectionId": { "type": "string" },
        "itemId": { "type": "string" },
        "language": { "type": "string" },
        "timestamp": { "type": "object", "format": "google-firestore-timestamp" },
        "page": { "type": "string" }
      },
      "required": ["type", "sectionId", "language", "timestamp", "page"]
    }
  },
  "firestore": {
    "/sections/{sectionId}": {
      "schema": "section",
      "description": "Top-level documentation sections."
    },
    "/sections/{sectionId}/items/{itemId}": {
      "schema": "item",
      "description": "Articles belonging to a specific section."
    },
    "/admins/{adminId}": {
      "schema": {
        "type": "object",
        "properties": {
          "email": { "type": "string" },
          "role": { "type": "string" }
        }
      },
      "description": "Administrator roles."
    },
    "/feedback/{feedbackId}": {
      "schema": "feedback",
      "description": "User-submitted feedback entries."
    }
  }
}
